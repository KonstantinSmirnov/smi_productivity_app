<div class="row">
    <div class="col-sm-12">
        <h2>
            <%= @current_project.title %>
            <small>
                <%= link_to edit_app_project_path(@current_project) do %>
                    <i class="fa fa-cog pull-right"></i>
                <% end %>
            </small>
        </h2>
    </div>
</div>
