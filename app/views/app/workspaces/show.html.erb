<div class="row">
    <div class="col-sm-12">
        <h2>
            <%= @current_workspace.title %>
            <small>
                <%= link_to edit_app_workspace_path(@current_workspace) do %>
                    <i class="fa fa-cog pull-right"></i>
                <% end %>
            </small>
        </h2>
    </div>
</div>
