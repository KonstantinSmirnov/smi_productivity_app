require 'test_helper'
include UserHelpers
include ProjectHelpers

class WorkspaceUsers < Capybara::Rails::TestCase

  # it "can add existing users in workspace"
  # it "sends email invitation to users not existingg in the system"
  # it "fails to add user if email has incorrect format"
  # it "fails to add user if user already persists in workspace"
  # it "can delete user"
  # it "can't delete itself from workspace"
  # it "only workspace admin can delete users"
  # it "admin can change user roles in workspace"
  # it "at least one admin should be in workspace"
  # it "if invitation sent, should be created a record that invitation sent?"
end
